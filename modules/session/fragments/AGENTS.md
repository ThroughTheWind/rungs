<!-- rungs:begin session@1.0.0 -->
## Session state

[`{{path}}`]({{path}}) is the live handoff — read it first, and treat
**Active constraints — do not reopen** as binding. Those are settled decisions, not suggestions;
reopening one needs a stated reason, not a fresh opinion.

Close a session with **`/close-session`**: it writes the dated archive note and rewrites the live
document to a resume point a cold session can act on.
<!-- rungs:end session -->
